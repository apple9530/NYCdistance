{
  "name": "nyc-commute-shed-web",
  "private": true,
  "type": "module",
  "description": "Static front end. Declared as an ES module package so Node can run the tests in tests/ against the same files the browser loads."
}
